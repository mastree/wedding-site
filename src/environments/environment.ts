import { LogLevel } from '../app/log-level.enum';
import { Environment } from './types';

export const environment: Environment = {
  API_URL: 'https://faiza.kamalshafi.me/api',
  LOG_LEVEL: LogLevel.INFO,
};
